// Import Styled
import styled from 'styled-components';

// Import Utils
import { rem } from '../../utils/helpers';

export const TitleBox = styled.h1`
    font-weight: 400;
    margin: ${rem(30)} 0px;
`;